version: "3.8"
services:
  rabbitmq-server:
    image: rabbitmq:3-management
    container_name: rabbitmq-server
    ports:
      - "5672:5672"
      - "15672:15672"
    environment:
      RABBITMQ_DEFAULT_USER: permafrost
      RABBITMQ_DEFAULT_PASS: permafrost
